const AuthController = require("./auth.controller");
const UserController = require("./user.controller");
const AccountUpgradeController = require("./accountUpgrade.controller");

module.exports = {
  AuthController,
  UserController,
  AccountUpgradeController,
};
